This lesson has been all about making use of pre-existing libraries.

It starts with generating random numbers using Random(), but it eventually goes into learning how to creating and editing files and directories, and reading those elements.